{
  "name": "lol-tracker-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "deploy:pages": "npm run build && npx wrangler pages deploy dist --project-name lol-tracker-frontend",
    "deploy:wrangler": "npm run build && npx wrangler deploy"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "vite": "^6.3.5",
    "vue": "^3.5.16",
    "lucide-vue-next": "^0.511.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17",
    "wrangler": "^4.94.0"
  }
}
